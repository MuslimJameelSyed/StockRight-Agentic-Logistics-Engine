# ============================================================================
# WAREHOUSE PUTAWAY SYSTEM - LOCAL DEPLOYMENT CONFIGURATION
# ============================================================================
# This file contains environment variables for local deployment.
# Copy this file to .env and fill in your actual values.
#
# IMPORTANT: Never commit .env to version control!
# ============================================================================

# ----------------------------------------------------------------------------
# QDRANT CONFIGURATION (Cloud API - Required)
# ----------------------------------------------------------------------------
# Get these from: https://cloud.qdrant.io/
QDRANT_URL=https://your-qdrant-instance.europe-west3-0.gcp.cloud.qdrant.io
QDRANT_API_KEY=your-qdrant-api-key-here
QDRANT_COLLECTION_NAME=PartSummary

# ----------------------------------------------------------------------------
# LOCAL MYSQL CONFIGURATION (Docker Container)
# ----------------------------------------------------------------------------
# These match the docker-compose.yml configuration
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_DATABASE=mydatabase_gdpr
MYSQL_USER=muslim
MYSQL_PASSWORD=warehouse_pass_2024        # ⚠️ CHANGE THIS IN PRODUCTION!
MYSQL_ROOT_PASSWORD=warehouse_root_2024   # ⚠️ CHANGE THIS IN PRODUCTION!

# ----------------------------------------------------------------------------
# OLLAMA CONFIGURATION (Local LLM)
# ----------------------------------------------------------------------------
# Default configuration for local Ollama Docker container
OLLAMA_HOST=localhost
OLLAMA_PORT=11434
OLLAMA_MODEL=llama3.2              # Options: llama3.2, mistral, phi3
OLLAMA_TEMPERATURE=0.1             # 0.0 = deterministic, 1.0 = creative
OLLAMA_MAX_TOKENS=1024             # Maximum response length
OLLAMA_REQUEST_TIMEOUT=60          # Timeout in seconds

# ----------------------------------------------------------------------------
# APPLICATION SETTINGS
# ----------------------------------------------------------------------------
LOG_LEVEL=INFO                     # Options: DEBUG, INFO, WARNING, ERROR
ENABLE_AUDIT_LOG=true              # Set to 'false' to disable audit logging
MAX_RETRIES=3                      # Number of retries for failed operations
REQUEST_TIMEOUT=30                 # Database query timeout in seconds

# ============================================================================
# NOTES:
# ============================================================================
#
# 1. QDRANT CONFIGURATION:
#    - This system uses Qdrant Cloud API (not local Qdrant)
#    - Get your URL and API key from https://cloud.qdrant.io/
#    - The collection should already be set up with your part patterns
#
# 2. MYSQL CONFIGURATION:
#    - These values match docker-compose.yml
#    - If you change passwords here, also update docker-compose.yml
#    - The database runs locally in a Docker container
#
# 3. OLLAMA CONFIGURATION:
#    - Ollama runs locally in a Docker container
#    - Available models: llama3.2 (2-4GB), mistral (4GB), phi3 (2GB)
#    - Download model with: docker exec warehouse-ollama ollama pull llama3.2
#
# 4. SECURITY:
#    - ALWAYS change default passwords in production
#    - Never commit .env file to git (already in .gitignore)
#    - Use strong, unique passwords for MySQL
#
# 5. GETTING STARTED:
#    - Copy this file: cp .env.local.example .env
#    - Fill in your Qdrant credentials
#    - Change MySQL passwords
#    - Run: docker-compose up -d
#    - Download model: docker exec warehouse-ollama ollama pull llama3.2
#    - Install dependencies: pip install -r requirements.txt
#    - Run: streamlit run app_local.py
#
# ============================================================================
